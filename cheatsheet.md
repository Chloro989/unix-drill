# UNIX コマンド チートシート

## 場所とファイル

| 目的 | コマンド |
|---|---|
| 今いる場所 | `pwd` |
| 一覧 | `ls` |
| 隠しファイルも | `ls -a` |
| 詳細（大きさ・日時） | `ls -l` |
| 移動 | `cd 名前` / `cd ..`（1つ上） / `cd`（ホーム） |
| 中身を出す | `cat ファイル` |
| 行番号付きで出す | `cat -n ファイル` |
| 連結して出す | `cat A B` |

## 中を覗く

| 目的 | コマンド |
|---|---|
| 先頭10行 | `head ファイル` |
| 先頭20行 | `head -n 20 ファイル` |
| 末尾5行 | `tail -n 5 ファイル` |
| 増えるのを見続ける | `tail -f ログ` |
| 1画面ずつ読む | `less ファイル` |
| 行数 | `wc -l ファイル` |
| 単語数 / バイト数 | `wc -w` / `wc -c` |
| 行・単語・バイト全部 | `wc ファイル` |

## 三本の管

```
          ┌──────────┐
  0 入力 →│ コマンド  │→ 1 出力
          │          │→ 2 エラー
          └──────────┘
```

| 記号 | 意味 |
|---|---|
| `> file` | 出力をファイルへ（**上書き**） |
| `>> file` | 出力をファイルへ（**追記**） |
| `< file` | ファイルを入力として流し込む |
| `2> file` | エラーだけをファイルへ |
| `2>/dev/null` | エラーを捨てる |
| `> file 2>&1` | 出力もエラーも同じファイルへ |
| `\|` | 前のコマンドの**出力**を次のコマンドの**入力**へ |
| `;` | 順に実行 |
| `&&` | 前が成功したら実行 |

## 探す（grep）

| 目的 | コマンド |
|---|---|
| 含む行を出す | `grep 語 file` |
| 件数 | `grep -c 語 file` |
| 含まない行 | `grep -v 語 file` |
| 大文字小文字を無視 | `grep -i 語 file` |
| 行番号を付ける | `grep -n 語 file` |
| どちらか | `grep -E "A\|B" file` |

## 正規表現

| 記号 | 意味 |
|---|---|
| `^` / `$` | 行頭 / 行末 |
| `.` | 任意の 1 文字 |
| `*` / `+` / `?` | 直前の 0回以上 / 1回以上 / 0か1回 |
| `[CG]` / `[^CG]` | どれか1文字 / それ以外 |
| `[0-9]{4}` | ちょうど 4 回 |
| `A\|B` | または |

`+ ? { } \|` は `grep -E` を付けて使う。
シェルの `*`（任意の並び）と正規表現の `*`（直前の繰り返し）は**別物**。

## 並べ替えと集計

| 目的 | コマンド |
|---|---|
| 並べ替え | `sort file` / `-n` 数値 / `-r` 逆順 / `-u` 重複消去 |
| 重複を数える | `sort file \| uniq -c` |
| 多い順に上位3 | `sort f \| uniq -c \| sort -rn \| head -n 3` |
| 列を取り出す | `cut -f 2 file` / `cut -d, -f 2 file` |
| 大文字に | `tr a-z A-Z` |
| 見出しを外す | `tail -n +2 file` |

## awk

| 目的 | コマンド |
|---|---|
| 1列目 | `awk '{print $1}' f` |
| 区切り指定 | `awk -F, '{print $2}' f` |
| 行数 | `awk 'END{print NR}' f` |
| 条件で絞る | `awk 'NR>1 && $3 > 100' f` |
| 合計 | `awk 'NR>1 {s+=$3} END{print s}' f` |
| 平均 | `awk 'NR>1 {s+=$3; n++} END{print s/n}' f` |

`$0` 行全体 / `NR` 行番号 / `NF` 列数。

## sed

| 目的 | コマンド |
|---|---|
| 置換（各行1つ） | `sed 's/A/B/' f` |
| 置換（すべて） | `sed 's/A/B/g' f` |
| n 行目 | `sed -n '5p' f` |
| 範囲 | `sed -n '1,3p' f` |
| 最終行 | `sed -n '$p' f` |
| 1行目を削除 | `sed '1d' f` |
| 行頭に足す | `sed 's/^/> /' f` |

`sed -i` は元のファイルを書き換える。macOS は `sed -i '' …`。

## 出題されやすい形

| 問い | 答え |
|---|---|
| カレントディレクトリの一覧を表示 | `ls` |
| ファイルAとBを連結してCへ | `cat A B > C` |

## 抜け出し方

| 状況 | キー |
|---|---|
| `less` / `man` の画面 | `q` |
| 終わらないコマンド | `Ctrl+C` |
| 入力待ちで戻らない | `Ctrl+D` |

## 間違えやすいところ

- 見出し行のある表を `wc -l` すると、**データ件数より 1 多い**。
- `>` は上書き、`>>` は追記。`>` の右に大事なファイルを書くと消える。
- `wc -l file` はファイル名も表示、`wc -l < file` は数字だけ。
- `head -20` は古い書き方。試験では `head -n 20`。
- シェルの `*`（何文字でも）と、正規表現の `*`（直前の繰り返し）は**別物**。
- 隠しファイルは名前がピリオドで始まる。`ls` では見えない。
- `uniq` の前には必ず `sort`。離れた重複は見逃す。
- `sort` は既定で文字順。`9` > `100` になる。数値なら `-n`。
- `sed` の `s///` は `g` を付けないと 1 行につき 1 つだけ。
- `awk` と `sed` のプログラムはシングルクォートで囲む。
- `sed -n` と `p` は対で使う。片方だけだと 2 回出るか何も出ない。
